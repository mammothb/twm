# Suppress ONLY the trivial-native-wrapper rule on the wrapper files.
# Other rules (security, correctness, complexity, style) still fire normally.
#
# The wrapper files below are pure P/Invoke boundary code: their job is to
# encode the native signature, default the unused parameters (wParam/lParam
# for PostThreadMessageW, etc.), and give callers a typed API surface.
# The wrapper IS the value, not dead code — that's why this rule's finding
# is intentionally suppressed here and only here.
sonar.issue.ignore.multicriteria=trivialNativeWrapper
sonar.issue.ignore.multicriteria.trivialNativeWrapper.ruleKey=csharpsquid:S4200
sonar.issue.ignore.multicriteria.trivialNativeWrapper.resourceKey=**/MessageLoop.cs,**/NativeMethods.cs,**/NativeMethods.Enums.cs,**/NativeMethods.Windowing.cs,**/ImmersiveShell.cs
